{# templates/page/dashboard.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">ClientHub</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="{{ path('dashboard') }}">Dashboard</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('clients') }}">Clients</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('factures') }}">Factures</a>
                    </li>
                    <li class="nav-item">
                    {# <a href="{{ path('logout') }}" class="btn btn-danger">Logout</a> #}

                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="text-center mt-4">
        <h1 class="mb-4">Bienvenue, User!</h1>
        <p>Utilisez la barre de navigation pour accéder aux clients et factures.</p>
    </div>
{% endblock %}
